# 官方客户端画像优化方案与实施结果

## 一、总判断

你的思路是正确的：

> 将 API Key 画像也升级为“同版本 CLI 在 API Key 认证模式下的真实行为”，可以把以后升级收敛为一次自动化抓包任务，由任务顺序覆盖认证、端点、协议和网络环境矩阵。

这与我此前结论不冲突。需要坚持的边界是：

- 可以统一客户端产品、版本管理、抓包工具和 Profile 引擎。
- 不能把 OAuth 与 API Key 合并成同一个最终画像。
- “一次任务”可以；“一份流量”不可以。

本方案采用“先消除架构债务，再直接升级默认值”的策略：第一阶段先完成共享常量隔离、
Profile Registry、配置可逆性、端点所有权、终态校验和回退能力建设，并保持当前默认行为；
架构债务消除且回归测试通过后，第二阶段基于已经完成的 Claude Code 2.1.220、Codex CLI
0.145.0 OAuth/API Key、HTTP/WS、direct/MITM 抓包，直接把默认画像升级为同版本 CLI
的对应认证模式，不设置 candidate 灰度阶段。旧 Profile 仅作为明确的 `previous` 回退项保留；
AnyRouter 恢复后的 A/B 属于上线后兼容性验证，不作为代码升级和默认值切换的前置条件。

### 实施状态（2026-07-26）

本方案已按“先消除架构债务，再直接升级默认画像”完成代码实施，未进行账号级灰度。

| 实施项 | 结果 |
|---|---|
| 共享数据所有权 | 已恢复 `pkg/claude/constants.go` 与 `defaultFingerprint` 的上游默认值；API Key 画像不再污染 OAuth、账号测试、模型同步或 Antigravity。 |
| Profile Registry | 已新增不可变 `ClientBuild + WireProfile + ReleasePointer`，按产品、认证、端点、传输和网络变体组合；每份画像带 ID、来源和 SHA-256 digest。 |
| 可逆配置 | 已停止静默删除 OAuth 冲突配置；旧值作为 dormant 数据保留，用户新启用冲突项时显式拒绝。 |
| 终态边界 | 已禁止 Official Egress HTTP 自动重定向，在实际发送前校验 Method、scheme、Host 和精确 Path；未知画像或 mode fail-closed。 |
| 服务级回退 | 已新增 `gateway.official_client_profiles.mode`，支持 `active` / `previous` 整体切换；历史账号级 profile 字段仅作 dormant 数据保留，禁止混用两套字段。 |
| 默认画像 | 已切换为 Claude Code CLI `2.1.220` 和 Codex CLI `0.145.0` 的 OAuth/API Key 对应认证模式。 |
| 协议策略 | Codex API Key mimic 仍强制 HTTP/SSE；API Key WS Profile 保存为 inactive，不会被路由层选中。 |
| 未完成外部验收 | AnyRouter 账号不可用，官方 CLI 直连与 Sub2API 转发 A/B 待恢复后执行；本文不将其标记为已通过。 |

三方工程师在改造前抓到了一个比初次评价更严重的问题：**Anthropic API Key Desktop 画像进入了共享全局常量，已经产生实际跨路径污染。**这是本轮将架构债务清理放在默认画像升级之前的直接原因。

---

## 二、改造前审计结论

| 维度 | OAuth Official Egress | API Key mimic |
|---|---|---|
| 产品边界 | 正确：平台级 OAuth 自动画像 | 正确：账号级、非官方客户端才触发 |
| 主体架构 | Profile、Resolver、Finalizer、Transport 分层较好 | 主体逻辑不差，OpenAI 侧隔离方式较好 |
| 协议正确性 | `/messages`、Responses HTTP/WS 主链路较完整 | Desktop 兼容有实测价值，但部分画像混入共享数据 |
| 运行时隔离 | 主链路严格、fail-closed | Anthropic 共享常量破坏了隔离 |
| 维护性 | 版本、平台、端点仍有耦合 | 常量、Header、Body、TLS 分散且存在命名漂移 |
| 回退能力 | 无服务开关、配置会被静默停用或删除 | mimic 可关闭，但全局常量污染不能随开关回退 |
| 综合评价 | 核心设计较优，接入和配置治理需改进 | 务实但不是综合最优，Anthropic 侧已有正确性缺陷 |

评价“最小侵入”不能只看代码行数。受条件严格保护的 C 类接入点，运行影响可能很小；一行全局 D 类值修改却可能污染十条路径。**所有权和影响半径比修改行数更重要。**

---

## 三、本轮已消除的架构债务

### 1. Claude Desktop 画像污染共享常量（已修复）

改造前 [Claude 共享常量](/Users/czs/Developer/sub2apiplus/backend/internal/pkg/claude/constants.go:98)同时承担了至少三种不同角色：

- 上游原有 Claude CLI 默认值。
- API Key Claude Desktop mimic。
- OAuth 非主路径和辅助功能默认值。

这导致：

- `defaultFingerprint` 组合出未抓包验证的 `2.1.209 + sdk-cli`：[identity_service.go](/Users/czs/Developer/sub2apiplus/backend/internal/service/identity_service.go:29)
- OAuth `count_tokens` 可能发送 Desktop 2.1.209/MacOS Header。
- 未启用 mimic 的账号测试也会读取 Desktop 值。
- Anthropic 模型同步读取 Desktop 值。
- Antigravity API Key 兼容网关的模型同步发送 Claude Desktop Header：[upstream_models.go](/Users/czs/Developer/sub2apiplus/backend/internal/service/upstream_models.go:303)

OAuth `/messages` 最终会被 2.1.218 Finalizer 修正，因此主模型请求没有最终污染；但辅助端点确实存在问题。

API Key 画像现已移入 Plus 私有 Registry，共享常量和 Identity 默认值已恢复，上述跨路径污染不再存在。

### 2. `count_tokens` 没有独立正式 Endpoint Contract（已隔离）

改造前 Anthropic Official Egress 只覆盖模型入口，不覆盖 `/v1/messages/count_tokens`：[official_egress_integration.go](/Users/czs/Developer/sub2apiplus/backend/internal/service/official_egress_integration.go:81)。

API Key `count_tokens` 也明确是有限兼容链路，没有完整官方样本。直接升级时采用以下边界：

1. 可以共享已经证明一致的 CLI `ClientBuild`，包括 UA、运行时和基础 Header。
2. 不得继承 `/messages` 的 Body Finalizer、beta 推导和缓存结构。
3. 当前使用明确命名的 generic compatibility Endpoint Contract，并保持 Body 保守转换。
4. 后续将 `count_tokens` 纳入自动抓包矩阵，获得样本后再升级为正式 Endpoint Contract。

### 3. OAuth 配置治理不可逆（已修复）

改造前 OAuth 账号会通过四处早退停用旧配置：

- [TLS](/Users/czs/Developer/sub2apiplus/backend/internal/service/account.go:1923)
- [Session ID](/Users/czs/Developer/sub2apiplus/backend/internal/service/account.go:1993)
- [Custom Base URL](/Users/czs/Developer/sub2apiplus/backend/internal/service/account.go:2013)
- [Cache TTL](/Users/czs/Developer/sub2apiplus/backend/internal/service/account.go:2039)

改造前下一次保存账号时还会删除旧字段：[配置归一化](/Users/czs/Developer/sub2apiplus/backend/internal/service/official_egress_profile.go:534)。

已改为：

- 冲突配置作为 dormant 数据保留。
- 用户明确尝试启用冲突功能时返回 400，并列出冲突原因。
- UI 显示“当前不生效”，而不是静默删除。
- 增加服务级紧急总开关和 `active/previous` Profile 指针；不引入 candidate 灰度层。
- 继续避免账号级任意版本选择，防止同服务内画像漂移。

### 4. OpenAI API Key 画像漂移（已收口）

本轮已同步处理：

- `cli_rs_0_125` 名称与实际发送的 0.144.1 不一致。
- Desktop 正式 Gateway 与后台账号测试可能走不同 TLS 路径。
- README 声称显式 TLS Profile 优先，但 Desktop 请求级判定会直接排除 TLS fingerprint。
- Body 解析失败时返回原始 Body，缺少 Official Egress 同等级的终态校验。

这些已由统一 Profile Registry 和统一账号测试路径收口。

### 5. 最终 Host/Path 与重定向边界（已加固）

改造前 Official Egress 主要验证入口路径和上游 Host，没有完整绑定最终实际 Path。本轮已默认禁止 HTTP 重定向，并校验最终 Method、scheme、Host 和精确 Path。

否则画像可能在构造时合法，实际发送时已变成另一个端点。

### 6. 版本 Bundle 过度耦合（已拆分）

`phase0-2026-07-24` 同时绑定 Claude 和 Codex。未来仅升级 Claude，也会让 OpenAI Profile 看起来一起变化。

现已拆为不可变 ID，例如：

```text
anthropic_claude_code_2_1_220_oauth_messages_http_direct
anthropic_claude_code_2_1_220_apikey_messages_http_direct
openai_codex_cli_0_145_0_oauth_responses_ws_direct
openai_codex_cli_0_145_0_apikey_responses_http_direct
anthropic_claude_desktop_2_1_209_apikey_messages_http_previous
openai_codex_desktop_0_144_0a4_apikey_responses_http_previous
```

发布 Bundle 只负责指向当前 `active/previous` 组合。

---

## 四、推荐的最终架构

### 1. 保留两个功能边界

激活策略必须独立：

```text
OAuth：
平台 + OAuth/SetupToken + 官方 Host + 支持端点
→ 自动应用服务绑定的 Official Profile

API Key：
API Key 账号 + 账号开关 + 非官方入站客户端
→ 应用服务绑定的同版本 CLI API Key Profile

API Key + 官方客户端入站：
→ 跳过 mimic，保留真实客户端请求
```

不能把 API Key 的“官方客户端跳过”规则复制到 OAuth，也不能让 OAuth 的强制 Profile 规则覆盖 API Key。

### 2. 建立组合式 Profile Registry

Profile 至少由以下维度组成：

```text
Provider
+ ClientProduct
+ ClientSurface
+ ClientVersion
+ AuthMode
+ EndpointContract
+ Transport
+ NetworkVariant
+ Runtime
+ ActualHost/Path
```

建议分层：

1. `ClientBuild`：版本、UA、运行时。
2. `SurfaceOverlay`：CLI/Desktop 的 entrypoint、originator、system、tools。
3. `AuthOverlay`：OAuth/API Key、beta、账号 Header。
4. `EndpointContract`：messages、count_tokens、responses、compact、input_tokens。
5. `TransportVariant`：HTTP/WS、direct/proxy、TLS、ALPN、HTTP/2。
6. `ActivationPolicy`：OAuth 自动启用、API Key 账号开关、官方客户端 skip、服务级紧急回退。

同版本 CLI 在 OAuth 与 API Key 间最多共享 `ClientBuild` 和经抓包证明一致的字段，其他层仍独立。
直接切换默认值不改变这一边界：API Key 不能直接引用 OAuth 的最终 Profile。

### 3. Profile 数据与代码分离

适合数据化：

- 版本及完整 UA。
- 静态 Header 与顺序。
- beta 列表及顺序。
- endpoint 能力声明。
- 受保护 Header 所有权。
- TLS 参数和 Transport 摘要。

继续由代码实现：

- Body 语义转换。
- 动态身份与生命周期。
- billing/metadata 同源计算。
- 重试重建。
- 签名阶段。
- WS 帧状态机。
- 连接池管理。

UA 模板只能用于抓包证明可参数化的部分。对外最终值仍应作为不可变整体保存，避免再次拼出不存在的客户端。

### 4. 统一最终发送管线

推荐管线：

```text
账号与激活策略解析
→ 选择不可变 Profile
→ 协议及 Body 语义转换
→ Body Finalizer
→ 生成 HeaderPlan + TransportPlan
→ 校验实际 Method/Host/Path
→ 应用 HeaderPlan
→ Sign（如需要）
→ HTTP Transport / WS Dialer
```

RoundTripper 只能承担最后的 HeaderPlan 应用和实际 URL 校验，不能承担完整 Body Finalizer。

原因：

- Body 需要同步 `Body/ContentLength/GetBody`。
- WS 数据帧不经过 HTTP RoundTripper。
- 动态身份不能在每次底层 RoundTrip 时重新生成。
- Go Transport 后续仍可能自动添加 Header，RoundTripper 不能单独保证完整 wire。
- 只有已经附加 Profile Context 的请求才能使用该层，`count_tokens` 不会因此自动获得覆盖。

---

## 五、统一版本后的抓包升级方案

可以把未来升级做成一个自动化任务，但任务内部应顺序运行，避免连接池和会话互相污染。

### Anthropic

```text
Claude Code 新版本
├─ OAuth /messages
├─ OAuth /count_tokens
├─ API Key /messages
└─ API Key /count_tokens
```

### OpenAI

```text
Codex CLI 新版本
├─ OAuth Responses HTTP
├─ OAuth Responses WebSocket
├─ OAuth compact/input_tokens
├─ API Key Responses
└─ API Key 的 HTTP/WS 实际选择
```

每个适用场景继续覆盖：

- direct：TLS、ALPN、连接复用。
- MITM：URL、Header、Body、WS 帧。
- proxy：实测与直连不同才建立独立画像。
- S1：冷启动。
- S2：连续多轮。
- S4：完整工具闭环。
- 官方平台和 Sub2API 公共 API 入口。
- 代表性中转站 A/B 作为发布后的兼容性复核，不作为默认值切换门槛。

自动化任务应生成：

- Capture Manifest。
- 规范化后的 Header/Body/TLS 摘要。
- OAuth/API Key 允许差异表。
- 未声明差异报告。
- 可直接发布的不可变 Profile 数据。
- Golden fixtures。
- 脱敏诊断和回滚清单。

这样以后是“一次命令完成一轮升级取证”。对已经取得正式样本的 Endpoint，完成契约测试后
直接更新 `active` 并把旧值保存为 `previous`；没有正式样本的 Endpoint 继续使用明确命名的
generic compatibility Contract，不阻塞已验证主端点升级。

---

## 六、对三方四步方案的最终取舍

### 直接采纳

1. 撤回 Anthropic API Key 对共享常量和 Identity 默认值的修改。
2. 先把现有 Desktop API Key 行为收口为 Plus 私有 Profile，消除共享污染且保持行为不变。
3. 把 Profile 数据与应用代码分离。
4. 保留 OAuth/API Key 两个 Finalizer 和激活策略。
5. 保存配置时显式报告冲突，不再静默删除。
6. 架构债务消除后，直接把 Claude/Codex API Key 默认画像升级为同版本 CLI API Key Profile。

### 调整后采纳

1. C 类不应全部变成细粒度账号开关，应集中到一个兼容性决策层。
2. OAuth 保持服务端绑定，但增加服务级紧急回退能力。
3. RoundTripper 只下沉 HeaderPlan 和最终 URL 校验。
4. `count_tokens` 可共享 CLI `ClientBuild`，但必须保留独立 generic Endpoint Contract；取得
   正式样本前不能机械继承 `/messages` 的 Body、beta 和缓存规则。

### 不建议采纳

1. 不要在共享常量污染、配置不可逆和发送路径分叉尚未解决时切换默认画像。
2. 不要直接复制 OAuth CLI 最终 Profile 给 API Key；只能共享已证实一致的 `ClientBuild`。
3. 不要仅移动 Header 常量，却继续调用共享的 `applyClaudeCodeMimicHeaders`。
4. 不要把 `api_key` 从全局脱敏词表移除。
5. 不要在 AnyRouter 等上游尚未验证时启用 Codex API Key WS；本轮仍保持 HTTP-only。

`api_key` 本来就是全局敏感字段。[全局脱敏修改](/Users/czs/Developer/sub2apiplus/backend/internal/util/logredact/redact.go:14)应拆成独立安全提交并单独回归，而不是缩小脱敏范围。

---

## 七、实施顺序与状态

### P0：冻结现状并建立安全边界（已完成）

1. 增加现状特征测试、抓包 Golden fixtures 和跨路径负向测试。
2. 恢复上游 Claude 共享常量和 Identity 所有权。
3. 把现有 Claude Desktop 2.1.209、Codex Desktop 0.144 API Key 行为迁入 Plus 私有 Profile，
   仅改变所有权，不改变当前默认输出。
4. 修复 OAuth `count_tokens`、普通账号测试、模型同步和 Antigravity 的画像边界。
5. 保留 dormant 配置，停止静默删除；用户显式启用冲突项时返回清晰错误。
6. 禁止或严格限制 Official Egress HTTP 重定向，并校验最终 Method/Host/Path。

### P1：彻底消除架构债务（已完成）

1. 建立不可变组合式 Profile Registry，拆分 `ClientBuild`、Auth、Endpoint 和 Transport。
2. 拆分 Claude/Codex 发布 Bundle，建立 `active/previous` 指针和服务级紧急回退开关。
3. 增加 Profile digest、抓包来源、适用端点和终态诊断。
4. 未知或不完整 Profile fail-closed，不得静默回落或拼装 UA。
5. 正式 Gateway 与账号测试复用完全相同的 Profile 解析、HeaderPlan、Body Finalizer 和 TLS 决策。
6. `count_tokens` 使用独立 generic Endpoint Contract；不得冒充 `/messages` 正式画像。
7. 架构债务阶段完成时必须通过完整后端测试，并证明非 mimic、OAuth、模型同步和 Antigravity
   不再受到 API Key Profile 影响。

### P2：直接升级默认画像（已完成）

P0、P1 全部完成后，不进行账号级灰度，直接执行：

1. Claude OAuth：`2.1.218 → 2.1.220`，旧版本保存为 `previous`。
2. Claude API Key mimic：`Desktop 2.1.209 → Claude Code CLI 2.1.220 API Key 模式`。
3. Codex OAuth：继续使用 0.145.0，并按最新抓包修正完整 UA 等静态字段。
4. Codex API Key mimic：`Desktop 0.144 → codex_exec 0.145.0 API Key 模式`。
5. Claude/Codex 的 OAuth 与 API Key Profile 共享经证明一致的 `ClientBuild` 和 TLS 数据，
   Auth、Endpoint、billing、beta、cache、WS Header 等差异继续独立。
6. Codex API Key 上游传输仍保持 HTTP-only；WS 画像保存但暂不激活。
7. 完成 Profile 契约、Header/Body/TLS、工具闭环、多轮会话和跨路径污染回归后，直接发布。

### P3：发布后兼容性验证与回退（待 AnyRouter 恢复）

1. AnyRouter 恢复后，对 Claude/Codex 执行官方 CLI 直连与 Sub2API 转发 A/B。
2. A/B 结果用于发现中转站特有兼容问题，不作为 P2 默认值切换的前置条件。
3. 若出现实质性故障，通过服务级开关整体回退到 `previous`，不得自动混用不同画像字段。

### P4：最终发送边界优化（本轮已完成安全部分）

本轮已把 Profile Context、Header 最终应用和 Method/scheme/Host/Path 校验放到 HTTP/WS 最终发送边界，并禁止 HTTP 自动重定向。Body Finalizer 仍保留在语义转换层，不下沉到 RoundTripper。

---

## 最终结论

最优方案不是“把两个功能合并”，而是：

> **保留 OAuth 与 API Key 的功能和协议边界，统一版本化 Profile 引擎、静态画像数据、最终发送框架和自动化抓包升级流程。**

你的“同版本 CLI + 一次自动化抓包任务”方向已直接用于本次正式升级：先彻底消除架构债务，
再依据已完成的真实抓包一次性把 Desktop 默认画像替换为同版本 CLI 对应认证模式，未设置 candidate 或账号级灰度阶段。
AnyRouter A/B 后置为兼容性验证，旧 Desktop Profile 仅作为整体 `previous` 回退项。

当前 OAuth 核心架构可以保留；API Key 主体也不需要推倒重来。真正应优先重构的是两者与上游共享代码的接触面、端点画像所有权、配置可逆性和 Profile 数据治理。
