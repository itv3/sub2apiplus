# Codex CLI v0.156.1 升级：阶段 1 工具改造审核

本次交付范围是方案第 4 节第 2 步的**工具开发与逐项自查**。按用户最新指示，完成此范围后停止，生产统一部署、正式升级和阶段 2 留待审核后继续。

## 1. 逐项完成情况

按 `R13-1、R13-3 → R15 → R12 → R7 → R6 → R2 → R4 → R11 → R8` 顺序实施。每项都有源码提交、冻结承接及自查报告，前项自查通过后才进入后项。

| 改造项 | 已实现并自查的行为 | 证据 |
| --- | --- | --- |
| R13-1、R13-3 | 连续升级链、六类故障复现基线、真实链登记及发布覆盖检查；默认单测与真实链分开。R3 保留改造前的拒绝断言，待阶段 2 翻绿。 | [R13 自查](codex-01561-r13-validation-20260923.json) |
| R15 工具与保护实现 | 出口从工具身份移到运维策略；实时准入、分别／共同阻断、两端路径保护、影响窗口与受控重建。隔离内核链的非授权出口业务包为 0。 | [R15 自查](codex-01561-r15-validation-20260924.json) |
| R12 | 子进程、心跳、上传及总等待均有界；中断后凭完整构建凭证从上传等待继续。ARM64 演练续跑时构建执行 0 次。 | [R12 自查](codex-01561-r12-validation-20260924.json) |
| R7 | 驱动按每轮版本、摘要、账号、路径和批准集合生成；不再复制上一轮常量。 | [R7 自查](codex-01561-r7-validation-20260924.json) |
| R6 | 官方证据导入后自动对齐账本；发布后中断可续作，固定事件幂等，原证据保持不变。 | [R6 自查](codex-01561-r6-validation-20260924.json) |
| R2 | 同源码只换镜像的候选可在原 Campaign 开后继 revision；按完整输入证明复用实现测试，拒绝伪造差异。 | [R2 自查](codex-01561-r2-validation-20260924.json) |
| R4 | VC-1～VC-3 父动作失败按可恢复、待审核、永久停线分支处理；按 COMMIT 与 reservation 状态确定续跑入口。 | [R4 自查](codex-01561-r4-validation-20260924.json) |
| R11 | 恢复段中已完成且可信的 Job 可复用；真实三段链执行数为 2→1→0、复用数为 0→1→2，ARM64 完成最终封存。 | [R11 自查](codex-01561-r11-validation-20260924.json) |
| R8 | 项目／Campaign／阶段预算到期只暂停；批准收据分别延期；两账中断幂等补齐；72 小时仅提醒；显式放弃才因预算转终态。 | [R8 自查](codex-01561-r8-validation-20260924.json) |

历史自查报告记录的是该项完成时的行为；R8 已替代 R4、R6 等早期报告中“预算到期停线”的旧口径。完整性、账务、请求与根因上限的永久停线规则仍保留。

## 2. R8 最终自查

- 本机最终定向回归：179 项通过，28.014 秒。
- ARM64 最终定向回归及三条真实链：182 项通过，259.043 秒。
- 最终部署／认证测试：17 项通过；两组 Go 冻结检查、`check-egress-spec-ci`、schema 和回退边界检查通过。
- 各套件存在覆盖重叠，不将上述数量相加为独立用例总数。

真实 CLI 链等待实际到期，在延期总账事件写入后强制终止进程，确认未闭合时拒绝派发，再补齐且重复提交不新增事件，最后在**原 Campaign** 派发第 2 批并实际执行一个动作。原计划及证据字节不变，两账延期事件各 1 条，外部上游请求 0。

专项检查覆盖三层独立延期、过期批准、无批准、暂停期间四类消费者拒绝、显式放弃中断、跨 revision 累计耗时、部分延期的连续暂停起点、历史秒级投影、实时快照竞态及阶段截止早于父截止的实际停止。

R8 的策略、采集行为、证据语义摘要与 R11 相同，仅控制层变化。新 schema 已用实际产物离线验证；旧工具可读旧数据，对新暂停／延期事件明确拒绝并保持字节不变。已写新事件的 Campaign 不能直接换回不兼容旧工具执行。

R8 源码提交：`3b4df92c1`、`96ee668a0`。冻结承接、源码摘要、日志摘要与自查发现的修复记录见 [R8 自查报告](codex-01561-r8-validation-20260924.json)。

## 3. 审核后的待办边界

1. **生产发布与 R15 实际网络迁移尚未执行。** 本轮只在本机与 ARM64 staging 私有挂载命名空间验证，未发布生产工具、驱动、镜像、Compose 或路由配置。生产仍要求 app 与 capture 使用 DMIT `69.63.195.102`。
2. 审核通过后，按原方案统一受监督部署、安装驱动，执行兼容／激活／pre-A3／发布认证。R15 两端保护、宿主保护、容器重建后的实时准入、实际依赖连通、抓包、重启恢复与性能检查均须在生产发布步骤完成，不能用本次隔离夹具代替。
3. **v0.156.1 正式升级尚未启动。** 目标已固定为上游 `rust-v0.156.1`，源码提交 `b412ff32c417f855c2b2d1581b77058eed87c84b`；已下载的目标二进制尚未执行。首次目标版本官方证据仍须重新采集。
4. 阶段 2 的 `R1 → R3 → R9 → R10 → R5 → R13-2` 尚未实施；如升级前置分析证明本次需要 R1 或 R5，按原方案处理其依赖。R14 仍按实际耗时决定。

本次暂停点为**阶段 1 工具开发自查完成，等待用户审核**；没有将隔离验证声明为生产验收，也没有将工具完成声明为仿真版本升级完成。
