{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sub2apiplus.local/schemas/codex-upgrade-supervisor-stop-v2.json",
  "title": "Codex 升级监督器停线收据（v2：显式携带动作输出绑定摘要或 null；v1 只读兼容，读侧归一化为 null）",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "event_type", "reason", "detected_at_utc", "detected_at_epoch", "owner_pid", "owner_nonce", "campaign_id", "phase", "action_outputs_sha256", "receipt_sha256"],
  "properties": {
    "schema_version": {"const": "codex-upgrade-supervisor-stop/v2"},
    "event_type": {"type": "string", "minLength": 1, "maxLength": 64},
    "reason": {"type": "string", "minLength": 1, "maxLength": 512},
    "detected_at_utc": {"type": "string", "format": "date-time"},
    "detected_at_epoch": {"type": "number", "exclusiveMinimum": 0},
    "owner_pid": {"type": "integer", "minimum": 1},
    "owner_nonce": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "campaign_id": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"},
    "phase": {"type": "string", "minLength": 1, "maxLength": 32},
    "action_outputs_sha256": {"oneOf": [{"type": "string", "pattern": "^[0-9a-f]{64}$"}, {"type": "null"}]},
    "receipt_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
  }
}
